# requirements.txt
# Python 3.7 + Windows compatible — tested with CARLA 0.9.13
#
# CORRECT INSTALL ORDER — follow exactly:
#
# Step 1 — downgrade setuptools (fixes gym build error):
#   pip install setuptools==59.5.0 wheel==0.37.1
#
# Step 2 — PyTorch (download .whl from browser, then install locally):
#   Browser: https://download.pytorch.org/whl/cu117/torch-1.13.1%2Bcu117-cp37-cp37m-win_amd64.whl
#   pip install C:\Downloads\torch-1.13.1+cu117-cp37-cp37m-win_amd64.whl
#
# Step 3 — safetensors FIRST (prevents puccinialin dependency bug):
#   pip install safetensors==0.3.3
#
# Step 4 — transformers without deps (avoids safetensors version conflict):
#   pip install transformers==4.30.2 --no-deps
#
# Step 5 — gym without deps (avoids extras_require validation error):
#   pip install gym==0.21.0 --no-deps
#
# Step 6 — everything else from this file:
#   pip install -r requirements.txt
#
# VERIFY:
#   python -c "import gym; print(gym.__version__)"
#   python -c "from stable_baselines3 import PPO; print('SB3 OK')"
#   python -c "from transformers import BlipProcessor; print('BLIP OK')"

# ── Core numeric ──────────────────────────────────────────────────────
numpy==1.21.6
opencv-python==4.5.5.64
Pillow==9.5.0

# ── Deep learning ─────────────────────────────────────────────────────
# torch installed manually (see Step 2 above)

# ── Stable Baselines3 ─────────────────────────────────────────────────
stable-baselines3==1.8.0

# ── Gym (installed via --no-deps in Step 5) ───────────────────────────
# Listed here so pip knows it is satisfied
gym==0.21.0

# ── HuggingFace / BLIP ────────────────────────────────────────────────
# safetensors and transformers installed manually (Steps 3 & 4)
safetensors==0.3.3
tokenizers==0.13.3
huggingface-hub==0.16.4
filelock==3.9.0
regex==2022.10.31
requests==2.28.2

# ── CARLA Python API ──────────────────────────────────────────────────
# Do NOT pip install carla.
# Copy egg to ./carla/ folder — handled automatically by environment.py

# ── Display ───────────────────────────────────────────────────────────
pygame==2.1.3

# ── Utilities ─────────────────────────────────────────────────────────
pandas==1.3.5
tqdm==4.64.1
tensorboard==2.11.2
protobuf==3.20.3