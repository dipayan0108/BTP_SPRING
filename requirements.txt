# requirements.txt
# Python 3.7 compatible — tested with CARLA 0.9.13
#
# INSTALL ORDER MATTERS:
#   1. torch first
#   2. then everything else
#
# pip install torch==1.13.1+cu117 -f https://download.pytorch.org/whl/torch_stable.html
# pip install -r requirements.txt

# ── Core numeric ──────────────────────────────────────────────────────
numpy==1.21.6               # last version with full py37 + torch 1.13 compat
opencv-python==4.7.0.72     # py37 compatible
Pillow==9.5.0               # py37 compatible

# ── Deep learning ─────────────────────────────────────────────────────
# Install torch MANUALLY before this file — see note above.
# torch==1.13.1   (install separately with CUDA wheel)

# ── Stable Baselines3 ─────────────────────────────────────────────────
# SB3 1.8.0 = last version that:
#   - uses gym (not gymnasium)
#   - supports Python 3.7
#   - supports torch 1.13.x
stable-baselines3==1.8.0

# ── Gym (NOT gymnasium) ───────────────────────────────────────────────
# gymnasium requires Python 3.8+, so we use gym
gym==0.21.0                 # version used by SB3 1.8.0 internally

# ── BLIP / HuggingFace ────────────────────────────────────────────────
# transformers 4.35.2 = last version with py37 support
# AND has BlipProcessor + BlipForConditionalGeneration
transformers==4.35.2
tokenizers==0.14.1          # must match transformers 4.35.x
huggingface-hub==0.19.4     # py37 compatible

# ── CARLA Python API ──────────────────────────────────────────────────
# Do NOT pip install carla — use the .egg file instead.
# Add this to sys.path in environment.py (already done):
#   sys.path.append('./carla/carla-0.9.13-py3.7-linux-x86_64.egg')

# ── Display ───────────────────────────────────────────────────────────
pygame==2.1.3               # py37 compatible

# ── Utilities ─────────────────────────────────────────────────────────
pandas==1.3.5               # last version with py37 support
tqdm==4.64.1                # py37 compatible
tensorboard==2.11.2         # py37 compatible, matches torch 1.13
protobuf==3.20.3            # required by tensorboard on py37